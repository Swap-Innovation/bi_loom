# Cursor API key — edit this file (never commit real keys)
AI_MODE=live
CURSOR_API_KEY=
CURSOR_MODEL=auto
CURSOR_API_BASE_URL=https://api.cursor.com
# Optional: worker pool name if your Cursor team uses sandbox pools
CURSOR_AGENT_POOL=

# OpenAI only if you prefer it over Cursor (optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
